[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: requires a running Postgres with pgvector (docker compose up postgres && alembic upgrade head)
addopts = -m "not integration"
